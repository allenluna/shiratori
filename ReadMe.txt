step 1: install python
step 2: install postgresql 
https://www.manageengine.com/au/products/desktop-central/software-installation/silent_install_PostgreSQL-16-(16.0).html

step 3: during postgresql installation set password to "qwerty"
step 4: open PG admin, you can see it to click windows button and type "PG admin"
step 5: after opening the pg admin right click PostgreSQL 16 and create database
set database name to "agkas"

step 6: pres ctrl + shift + ` then type "pip install -r requirements.txt"
step 7: in the terminal type "python main.py" to run the application the you can see 
the localhost that given in the terminal
